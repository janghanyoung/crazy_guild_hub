import PageContainer from "@/components/ui/PageContainer";
import SectionTitle from "@/components/ui/SectionTitle";

export default function GuidesPage() {
  return (
    <PageContainer>
      <SectionTitle title="공략집" description="레이드, 내실, 업적 공략을 모아둡니다." />
    </PageContainer>
  );
}